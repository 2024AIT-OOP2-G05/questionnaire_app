<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>教科編集</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='base-style.css') }}">
</head>
<body>
    <h1>教科を編集</h1>
    <ul class="breadcrumb">
        <li><a href="{{ url_for('index') }}">HOME</a></li>
        <li><a href="{{ url_for('favorite_subject.list') }}">教科一覧</a></li>
        <li>教科編集</li>
    </ul>

    <form action="{{ url_for('favorite_subject.edit', favorite_subject_id=favorite_subject.id) }}" method="post">
        <label>名前: <input type="text" name="name" value="{{ favorite_subject.name }}" required></label><br>
        <label>教科: <input type="number" step="1" name="favorite_subject" value="{{ favorite_subject.favorite_subject }}" required></label><br>
        <button type="submit">保存</button>
    </form>
</body>
</html>